const Movies = (props) => {
  console.log(props);
  return <img src={props.images} alt="images" />;
};

export default Movies;

// import React from "react";

// function Movies(props) {
//   return <div>{props.images}</div>;
// }

// export default Movies;
